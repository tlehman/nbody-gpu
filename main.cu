#include <stdio.h>


struct body {
    float radius;      // radius
    float x,y,z;       // position
    float vx, vy, vz;  // velocity
    float m;           // mass
};

const int n = 3;

int main() {
    // 1. Set up initial conditions
    body bodies[n];

    // 2. Run physics loop
    return 0;
}